import React from "react";
import styled from "styled-components";

const StyledPixels = styled.div`
  width: 100%;
  height: 100%;
  padding: 30px;
`;

const Pixels = () => {
  return <StyledPixels>Meow</StyledPixels>;
};

export default Pixels;
