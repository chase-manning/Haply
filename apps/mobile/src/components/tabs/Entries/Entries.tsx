import React, { Component } from "react";
import styled from "styled-components";

export default class Entries extends Component {
  render() {
    return <div data-testid="Entries">Entries Component</div>;
  }
}
